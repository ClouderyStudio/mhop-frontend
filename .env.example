# ===== MHOP 前端环境变量模板 =====
# 复制为 .env.local（开发）或 .env.production（生产构建）后按需填写。
# 所有 VITE_ 前缀变量都会被编译进前端产物，属于公开信息，请勿放入密钥。

# 后端 API 基地址。MHOP 全部接口都在 /mhop 前缀下，请写到 /mhop 为止。
# 留空（默认）：使用 /mhop，即同源反向代理或 vite 开发代理。
# 例：VITE_MHOP_API_BASE_URL=https://api.example.com/mhop
VITE_MHOP_API_BASE_URL=

# 图片等静态资源基地址，仅当图片与 API 不同源或走 CDN 时填写。
# 留空时：API 为绝对地址则自动取其源（scheme://host），否则保持同源相对路径。
# 例：VITE_MHOP_ASSET_BASE_URL=https://cdn.example.com
VITE_MHOP_ASSET_BASE_URL=

# vite 开发服务器反向代理目标（仅开发期生效，生产由 Nginx 等承担）。
VITE_MHOP_DEV_PROXY_TARGET=https://127.0.0.1:7288

# 开发服务器自签证书目录，相对仓库根目录；证书不存在时自动回退 HTTP。
VITE_MHOP_CERT_DIR=certs
